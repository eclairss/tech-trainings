const CardButton = {
    'VIEW TRAILER': 'View Trailers', 
    'GET TICKET': 'Get Ticket'
};

const NavBarLinks = {
    'LOGIN': 'Login', 
    'REGISTER': 'Register',
    'MOVIES': 'Movies', 
    'ADMIN' : 'Admin',
    'BRANCHES': 'Branches', 
    'LOGOUT': 'Logout'
};

const Modules = {
    "BRANCH": "Branch",
    "MOVIE": "Movie",
    "PAYMENT": "Payment"
}

const UNIQUE = Date.now();

const Options = {
    MAX: "max", 
    MIN: "min", 
    RANDOM: "random"
};

module.exports = {NavBarLinks,CardButton,UNIQUE,Modules,Options};